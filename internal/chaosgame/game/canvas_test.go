package game

import (
	"testing"
)

// empty test for now, used under development to make sure the canvas transform is setup correctly...
func TestCanvasTransform(t *testing.T) {
	// transform := getCanvasTransform(100, 100, model.Vector2D{0, 0}, model.Vector2D{1, 1})
	// result := transform.Transform(&model.Vector2D{0, 1})
	// t.Fatalf("result x:%v y:%v", result.Y, result.X)
}
